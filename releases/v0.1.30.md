## Summary
- Synchronized all packages to version 0.1.30
- No functional changes or API modifications

## Changes
- Updated version metadata across the monorepo to ensure all packages (`@gonvex/cli`, `@gonvex/client`, `@gonvex/protocol`, `@gonvex/react`, and `create-gonvex`) are aligned at v0.1.30

## Technical Notes
- Maintenance release containing only `package.json` version bumps with no code changes
- Published to maintain version consistency across the npm workspace
