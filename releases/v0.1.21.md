## Summary
- Eliminated contract divergence when syncing with legacy runtimes that omit ready-frame digests
- Client now stamps locally verified digests into emitted frames to ensure protocol uniformity
- Routine version alignment across all packages to 0.1.21

## Changes
- **Protocol Compatibility**: When processing ready frames from legacy peers that do not include a digest, the client now injects the locally verified digest before emitting to consumers. This ensures downstream code receives a consistent structure regardless of the remote peer's protocol generation.

## Technical Notes
- Monorepo-wide version bump to 0.1.21 for `@gonvex/client`, `@gonvex/cli`, `create-gonvex`, `@gonvex/protocol`, and `@gonvex/react`
- Modification localized to sync message emission logic in `packages/client/src/index.ts`
