{
  "name": "Browser_Prompt",
  "description": "Browser_Prompt shows a prompt dialog in the user's browser to collect input.\nThis function is never actually called - it exists only for schema generation.\nThe actual execution is intercepted by ExecuteToolWithContext.",
  "parameters": {
    "type": "object",
    "properties": {
      "message": {
        "type": "string"
      }
    },
    "required": [
      "message"
    ]
  }
}